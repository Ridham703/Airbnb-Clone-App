# Project Rules

## Git Workflow
- After completing each task step or milestone, automatically create a descriptive git commit and push the changes to `origin main` using PowerShell-compatible syntax (`git add . ; git commit -m "..." ; git push`).
